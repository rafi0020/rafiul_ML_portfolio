# Portfolio consistency verification

This report supersedes the earlier template-redesign QA notes. Historical screenshots and counts are not evidence for this revision.

## Changes

- Preserved all 29 case studies, stable project IDs, and the four featured projects.
- Derived headline statistics from content: case studies, journal articles, and preprints are distinct.
- Added shared maturity and context fields, parent-system labels, typed implementation facts and reported results, and source-linked research records.
- Corrected unsupported guarantees, conflicting model descriptions, R&D/production labels, research topology, and misleading destination labels.
- Replaced active workflow artwork with 29 conceptual diagrams and matching PNG social previews. Added readable HTML workflow stages and explicit captions.
- Corrected contact-window feedback, hidden-navigation focus behavior, mobile-menu focus scope, storage failure handling, and repeated screen-reader announcements.
- Prerendered all 35 routes with page-specific content and metadata; generated a complete canonical sitemap and noindex fallback.

## Verification

- Nine content regression tests passed.
- Production build and server rendering passed.
- All 35 generated routes have a main landmark, one h1, unique DOM IDs, matching canonical metadata, and valid local links/assets.
- Homepage featured selection remains exactly four projects; research preview remains 2024 and 2025.
- Diagram text bounds and representative PNG outputs inspected.
- Changed text files and generated text output scanned for private-key signatures, provider-token patterns, private IP addresses, and credential-bearing service URLs; no matches found. This is a signature scan, not a security certification.
- Both résumé files match the user-provided PDFs.
- Local browser preview was blocked by the browser's URL policy. This report does not certify viewport, theme, keyboard, or console behavior from static checks alone. Live interaction results are reported separately after deployment.

## Evidence limits

Extracted internal applications were not executed. Source inspection does not establish deployment scale, runtime reliability, clinical validity, calibrated model confidence, or passing-test totals. Removed unsupported figures must only be restored with supporting evidence and an evaluation context.
